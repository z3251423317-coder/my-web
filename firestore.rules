rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /settings/sheets {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /screens/current {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
